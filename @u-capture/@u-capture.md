---
title: u-capture
---

```datacards
TABLE title, source, description
FROM "@u-capture"
WHERE file.folder = "@u-capture"
AND file.name != "@u-capture"

// Settings
preset: portrait
imageProperty: cover
```